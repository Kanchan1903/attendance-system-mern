# Environment variables (project root)
# Copy this file to `../.env` from `server/` (i.e. repo root `.env`) and fill in values.

PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/attendance_system
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173
UPLOAD_DIR=uploads
